
import { command, option } from '@radic/console';
import { Command } from '../Command';


@command('unlink', 'Does the oposite of [link]. This removes the directory')
export default class UnlinkCommand extends Command {

}

