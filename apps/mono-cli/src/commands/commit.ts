import { arg, command, option } from '@radic/console';
import { Command } from '../Command';


@command('commit', 'Git add all & commit')
export default class extends Command {

    async handle() {

    }
}
