export declare class ArrayCollection<Type> extends Array<Type> implements Array<Type> {
    /**
     * Create a new collection instance.
     *
     * @param items
     */
    constructor(...items: Type[]);
}
